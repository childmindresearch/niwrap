# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

ROBUSTFOV_METADATA = Metadata(
    id="ba96504ba58594a00f41b8ac2107f15c773bd4a9",
    name="robustfov",
    container_image_type="docker",
    container_image_tag="mcin/fsl:6.0.5",
)


class RobustfovOutputs(typing.NamedTuple):
    """
    Output object returned when calling `robustfov(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    output_roi_volume: OutputPathType | None
    """ROI volume output"""
    output_matrix_file: OutputPathType | None
    """Matrix output (ROI to full FOV)"""


def robustfov(
    input_file: InputPathType,
    output_image: InputPathType | None = None,
    brain_size: float | int | None = None,
    matrix_output: str | None = None,
    debug_flag: bool = False,
    verbose_flag: bool = False,
    runner: Runner | None = None,
) -> RobustfovOutputs:
    """
    robustfov by University of Oxford (Mark Jenkinson).
    
    Reduce FOV of image to remove lower head and neck.
    
    More information: https://fsl.fmrib.ox.ac.uk/fsl/fslwiki/robustfov
    
    Args:
        input_file: Input image file.
        output_image: ROI volume output name.
        brain_size: Size of the brain in z-dimension (default 170mm).
        matrix_output: Matrix output name (ROI to full FOV).
        debug_flag: Turn on debugging output.
        verbose_flag: Switch on diagnostic messages.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `RobustfovOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(ROBUSTFOV_METADATA)
    cargs = []
    cargs.append("robustfov")
    cargs.append("[OPTIONS]")
    cargs.append("-i")
    cargs.extend(["-i", execution.input_file(input_file)])
    cargs.append("[-r")
    cargs.append("OUTPUT_IMAGE]")
    ret = RobustfovOutputs(
        root=execution.output_file("."),
        output_roi_volume=execution.output_file(f"{pathlib.Path(output_image).name}.nii.gz", optional=True) if output_image is not None else None,
        output_matrix_file=execution.output_file(f"{matrix_output}.txt", optional=True) if matrix_output is not None else None,
    )
    execution.run(cargs)
    return ret


__all__ = [
    "ROBUSTFOV_METADATA",
    "RobustfovOutputs",
    "robustfov",
]
