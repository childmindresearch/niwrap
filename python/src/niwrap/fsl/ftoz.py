# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

FTOZ_METADATA = Metadata(
    id="76cfc0dbfbf74693bcd33da5b1e25a4417be82b1",
    name="ftoz",
)


class FtozOutputs(typing.NamedTuple):
    """
    Output object returned when calling `ftoz(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    output_zscores: OutputPathType | None
    """File containing the converted Z-scores"""


def ftoz(
    input_file: InputPathType,
    dof1: float | int,
    dof2: float | int,
    output_file: InputPathType | None = "zstats",
    help_flag: bool = False,
    runner: Runner | None = None,
) -> FtozOutputs:
    """
    ftoz by Unknown.
    
    Convert F-statistics to Z-scores.
    
    Args:
        input_file: File containing F-statistics.
        dof1: Degrees of freedom 1 for F-to-Z conversion.
        dof2: Degrees of freedom 2 for F-to-Z conversion.
        output_file: Output file for Z-scores.
        help_flag: Display this help and exit.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `FtozOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(FTOZ_METADATA)
    cargs = []
    cargs.append("ftoz")
    cargs.append(execution.input_file(input_file))
    cargs.append(str(dof1))
    cargs.append(str(dof2))
    if output_file is not None:
        cargs.extend(["-zout", execution.input_file(output_file)])
    ret = FtozOutputs(
        root=execution.output_file("."),
        output_zscores=execution.output_file(f"{pathlib.Path(output_file).name}", optional=True) if output_file is not None else None,
    )
    execution.run(cargs)
    return ret


__all__ = [
    "FTOZ_METADATA",
    "FtozOutputs",
    "ftoz",
]
