# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import *


MRVIEW_METADATA = Metadata(
    id="8394d2b93b93dd2e7d2dee50f08a9cf5662871b7",
    name="mrview",
    container_image_type="docker",
    container_image_tag="mrtrix3/mrtrix3:3.0.4",
)


class ModeOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Mode.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Mode:
    """
    Switch to view mode specified by the integer index, as per the view menu.
    """
    index: int
    """Switch to view mode specified by the integer index, as per the view
    menu."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-mode")
        cargs.append(str(self.index))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ModeOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ModeOutputs`).
        """
        ret = ModeOutputs(
            root=execution.output_file("."),
        )
        return ret


class LoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Load.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Load:
    """
    Load image specified and make it current.
    """
    image_: InputPathType
    """Load image specified and make it current."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-load")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> LoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `LoadOutputs`).
        """
        ret = LoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class ResetOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Reset.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Reset:
    """
    Reset the view according to current image. This resets the FOV, projection and focus.
    """
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-reset")
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ResetOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ResetOutputs`).
        """
        ret = ResetOutputs(
            root=execution.output_file("."),
        )
        return ret


class FovOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Fov.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Fov:
    """
    Set the field of view, in mm.
    """
    value: float | int
    """Set the field of view, in mm."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-fov")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> FovOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `FovOutputs`).
        """
        ret = FovOutputs(
            root=execution.output_file("."),
        )
        return ret


class FocusOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Focus.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Focus:
    """
    Either set the position of the crosshairs in scanner coordinates, with the new position supplied as a comma-separated list of floating-point values or show or hide the focus cross hair using a boolean value as argument.
    """
    x_y_z_or_boolean: str
    """Either set the position of the crosshairs in scanner coordinates, with
    the new position supplied as a comma-separated list of floating-point values
    or show or hide the focus cross hair using a boolean value as argument."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-focus")
        cargs.append(self.x_y_z_or_boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> FocusOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `FocusOutputs`).
        """
        ret = FocusOutputs(
            root=execution.output_file("."),
        )
        return ret


class TargetOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Target.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Target:
    """
    Set the target location for the viewing window (the scanner coordinate that will appear at the centre of the viewing window
    """
    x_y_z: list[float | int]
    """Set the target location for the viewing window (the scanner coordinate
    that will appear at the centre of the viewing window"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-target")
        cargs.extend(map(str, self.x_y_z))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TargetOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TargetOutputs`).
        """
        ret = TargetOutputs(
            root=execution.output_file("."),
        )
        return ret


class VoxelOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Voxel.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Voxel:
    """
    Set the position of the crosshairs in voxel coordinates, relative the image currently displayed. The new position should be supplied as a comma-separated list of floating-point values.
    """
    x_y_z: list[float | int]
    """Set the position of the crosshairs in voxel coordinates, relative the
    image currently displayed. The new position should be supplied as a
    comma-separated list of floating-point values."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-voxel")
        cargs.extend(map(str, self.x_y_z))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> VoxelOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `VoxelOutputs`).
        """
        ret = VoxelOutputs(
            root=execution.output_file("."),
        )
        return ret


class VolumeOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Volume.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Volume:
    """
    Set the volume index for the image displayed, as a comma-separated list of integers.
    """
    idx: list[int]
    """Set the volume index for the image displayed, as a comma-separated list
    of integers."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-volume")
        cargs.extend(map(str, self.idx))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> VolumeOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `VolumeOutputs`).
        """
        ret = VolumeOutputs(
            root=execution.output_file("."),
        )
        return ret


class PlaneOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Plane.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Plane:
    """
    Set the viewing plane, according to the mappping 0: sagittal; 1: coronal; 2: axial.
    """
    index: int
    """Set the viewing plane, according to the mappping 0: sagittal; 1: coronal;
    2: axial."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-plane")
        cargs.append(str(self.index))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> PlaneOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `PlaneOutputs`).
        """
        ret = PlaneOutputs(
            root=execution.output_file("."),
        )
        return ret


class LockOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Lock.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Lock:
    """
    Set whether view is locked to image axes (0: no, 1: yes).
    """
    yesno: str
    """Set whether view is locked to image axes (0: no, 1: yes)."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-lock")
        cargs.append(self.yesno)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> LockOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `LockOutputs`).
        """
        ret = LockOutputs(
            root=execution.output_file("."),
        )
        return ret


class SelectImageOutputs(typing.NamedTuple):
    """
    Output object returned when calling `SelectImage.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class SelectImage:
    """
    Switch to image number specified, with reference to the list of currently loaded images.
    """
    index: int
    """Switch to image number specified, with reference to the list of currently
    loaded images."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-select_image")
        cargs.append(str(self.index))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> SelectImageOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `SelectImageOutputs`).
        """
        ret = SelectImageOutputs(
            root=execution.output_file("."),
        )
        return ret


class AutoscaleOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Autoscale.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Autoscale:
    """
    Reset the image scaling to automatically determined range.
    """
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-autoscale")
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> AutoscaleOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `AutoscaleOutputs`).
        """
        ret = AutoscaleOutputs(
            root=execution.output_file("."),
        )
        return ret


class InterpolationOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Interpolation.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Interpolation:
    """
    Enable or disable image interpolation in main image.
    """
    boolean: str
    """Enable or disable image interpolation in main image."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-interpolation")
        cargs.append(self.boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> InterpolationOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `InterpolationOutputs`).
        """
        ret = InterpolationOutputs(
            root=execution.output_file("."),
        )
        return ret


class ColourmapOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Colourmap.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Colourmap:
    """
    Switch the image colourmap to that specified, as per the colourmap menu.
    """
    index: int
    """Switch the image colourmap to that specified, as per the colourmap
    menu."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-colourmap")
        cargs.append(str(self.index))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ColourmapOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ColourmapOutputs`).
        """
        ret = ColourmapOutputs(
            root=execution.output_file("."),
        )
        return ret


class NoannotationsOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Noannotations.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Noannotations:
    """
    Hide all image annotation overlays
    """
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-noannotations")
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> NoannotationsOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `NoannotationsOutputs`).
        """
        ret = NoannotationsOutputs(
            root=execution.output_file("."),
        )
        return ret


class CommentsOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Comments.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Comments:
    """
    Show or hide image comments overlay.
    """
    boolean: str
    """Show or hide image comments overlay."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-comments")
        cargs.append(self.boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> CommentsOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `CommentsOutputs`).
        """
        ret = CommentsOutputs(
            root=execution.output_file("."),
        )
        return ret


class VoxelinfoOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Voxelinfo.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Voxelinfo:
    """
    Show or hide voxel information overlay.
    """
    boolean: str
    """Show or hide voxel information overlay."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-voxelinfo")
        cargs.append(self.boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> VoxelinfoOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `VoxelinfoOutputs`).
        """
        ret = VoxelinfoOutputs(
            root=execution.output_file("."),
        )
        return ret


class OrientationlabelOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Orientationlabel.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Orientationlabel:
    """
    Show or hide orientation label overlay.
    """
    boolean: str
    """Show or hide orientation label overlay."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-orientationlabel")
        cargs.append(self.boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OrientationlabelOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OrientationlabelOutputs`).
        """
        ret = OrientationlabelOutputs(
            root=execution.output_file("."),
        )
        return ret


class ColourbarOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Colourbar.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Colourbar:
    """
    Show or hide colourbar overlay.
    """
    boolean: str
    """Show or hide colourbar overlay."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-colourbar")
        cargs.append(self.boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ColourbarOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ColourbarOutputs`).
        """
        ret = ColourbarOutputs(
            root=execution.output_file("."),
        )
        return ret


class ImagevisibleOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Imagevisible.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Imagevisible:
    """
    Show or hide the main image.
    """
    boolean: str
    """Show or hide the main image."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-imagevisible")
        cargs.append(self.boolean)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ImagevisibleOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ImagevisibleOutputs`).
        """
        ret = ImagevisibleOutputs(
            root=execution.output_file("."),
        )
        return ret


class IntensityRangeOutputs(typing.NamedTuple):
    """
    Output object returned when calling `IntensityRange.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class IntensityRange:
    """
    Set the image intensity range to that specified.
    """
    min_max: list[float | int]
    """Set the image intensity range to that specified."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-intensity_range")
        cargs.extend(map(str, self.min_max))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> IntensityRangeOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `IntensityRangeOutputs`).
        """
        ret = IntensityRangeOutputs(
            root=execution.output_file("."),
        )
        return ret


class SizeOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Size.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Size:
    """
    Set the size of the view area, in pixel units.
    """
    width_height: list[int]
    """Set the size of the view area, in pixel units."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-size")
        cargs.extend(map(str, self.width_height))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> SizeOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `SizeOutputs`).
        """
        ret = SizeOutputs(
            root=execution.output_file("."),
        )
        return ret


class PositionOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Position.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Position:
    """
    Set the position of the main window, in pixel units.
    """
    x_y: list[int]
    """Set the position of the main window, in pixel units."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-position")
        cargs.extend(map(str, self.x_y))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> PositionOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `PositionOutputs`).
        """
        ret = PositionOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayLoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayLoad.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayLoad:
    """
    Loads the specified image on the overlay tool.
    """
    image_: InputPathType
    """Loads the specified image on the overlay tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.load")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayLoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayLoadOutputs`).
        """
        ret = OverlayLoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayOpacityOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayOpacity.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayOpacity:
    """
    Sets the overlay opacity to floating value [0-1].
    """
    value: float | int
    """Sets the overlay opacity to floating value [0-1]."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.opacity")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayOpacityOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayOpacityOutputs`).
        """
        ret = OverlayOpacityOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayColourmapOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayColourmap.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayColourmap:
    """
    Sets the colourmap of the overlay as indexed in the colourmap dropdown menu.
    """
    index: int
    """Sets the colourmap of the overlay as indexed in the colourmap dropdown
    menu."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.colourmap")
        cargs.append(str(self.index))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayColourmapOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayColourmapOutputs`).
        """
        ret = OverlayColourmapOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayColourOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayColour.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayColour:
    """
    Specify a manual colour for the overlay, as three comma-separated values
    """
    r_g_b: list[float | int]
    """Specify a manual colour for the overlay, as three comma-separated
    values"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.colour")
        cargs.extend(map(str, self.r_g_b))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayColourOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayColourOutputs`).
        """
        ret = OverlayColourOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayIntensityOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayIntensity.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayIntensity:
    """
    Set the intensity windowing of the overlay
    """
    min_max: list[float | int]
    """Set the intensity windowing of the overlay"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.intensity")
        cargs.extend(map(str, self.min_max))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayIntensityOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayIntensityOutputs`).
        """
        ret = OverlayIntensityOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayThresholdMinOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayThresholdMin.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayThresholdMin:
    """
    Set the lower threshold value of the overlay
    """
    value: float | int
    """Set the lower threshold value of the overlay"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.threshold_min")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayThresholdMinOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayThresholdMinOutputs`).
        """
        ret = OverlayThresholdMinOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayThresholdMaxOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayThresholdMax.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayThresholdMax:
    """
    Set the upper threshold value of the overlay
    """
    value: float | int
    """Set the upper threshold value of the overlay"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.threshold_max")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayThresholdMaxOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayThresholdMaxOutputs`).
        """
        ret = OverlayThresholdMaxOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayNoThresholdMinOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayNoThresholdMin.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayNoThresholdMin:
    """
    Disable the lower threshold for the overlay
    """
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.no_threshold_min")
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayNoThresholdMinOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayNoThresholdMinOutputs`).
        """
        ret = OverlayNoThresholdMinOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayNoThresholdMaxOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayNoThresholdMax.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayNoThresholdMax:
    """
    Disable the upper threshold for the overlay
    """
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.no_threshold_max")
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayNoThresholdMaxOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayNoThresholdMaxOutputs`).
        """
        ret = OverlayNoThresholdMaxOutputs(
            root=execution.output_file("."),
        )
        return ret


class OverlayInterpolationOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OverlayInterpolation.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OverlayInterpolation:
    """
    Enable or disable overlay image interpolation.
    """
    value: str
    """Enable or disable overlay image interpolation."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-overlay.interpolation")
        cargs.append(self.value)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OverlayInterpolationOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OverlayInterpolationOutputs`).
        """
        ret = OverlayInterpolationOutputs(
            root=execution.output_file("."),
        )
        return ret


class RoiLoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `RoiLoad.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class RoiLoad:
    """
    Loads the specified image on the ROI editor tool.
    """
    image_: InputPathType
    """Loads the specified image on the ROI editor tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-roi.load")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> RoiLoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `RoiLoadOutputs`).
        """
        ret = RoiLoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class RoiOpacityOutputs(typing.NamedTuple):
    """
    Output object returned when calling `RoiOpacity.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class RoiOpacity:
    """
    Sets the overlay opacity to floating value [0-1].
    """
    value: float | int
    """Sets the overlay opacity to floating value [0-1]."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-roi.opacity")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> RoiOpacityOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `RoiOpacityOutputs`).
        """
        ret = RoiOpacityOutputs(
            root=execution.output_file("."),
        )
        return ret


class RoiColourOutputs(typing.NamedTuple):
    """
    Output object returned when calling `RoiColour.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class RoiColour:
    """
    Sets the colour of the ROI overlay
    """
    r_g_b: list[float | int]
    """Sets the colour of the ROI overlay"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-roi.colour")
        cargs.extend(map(str, self.r_g_b))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> RoiColourOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `RoiColourOutputs`).
        """
        ret = RoiColourOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyLoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyLoad.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyLoad:
    """
    Load the specified tracks file into the tractography tool.
    """
    tracks: InputPathType
    """Load the specified tracks file into the tractography tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.load")
        cargs.append(execution.input_file(self.tracks))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyLoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyLoadOutputs`).
        """
        ret = TractographyLoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyThicknessOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyThickness.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyThickness:
    """
    Line thickness of tractography display, [-1.0, 1.0], default is 0.0.
    """
    value: float | int
    """Line thickness of tractography display, [-1.0, 1.0], default is 0.0."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.thickness")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyThicknessOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyThicknessOutputs`).
        """
        ret = TractographyThicknessOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyGeometryOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyGeometry.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyGeometry:
    """
    The geometry type to use when rendering tractograms (options are: pseudotubes, lines, points)
    """
    value: typing.Literal["value"]
    """The geometry type to use when rendering tractograms (options are:
    pseudotubes, lines, points)"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.geometry")
        cargs.append(self.value)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyGeometryOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyGeometryOutputs`).
        """
        ret = TractographyGeometryOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyOpacityOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyOpacity.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyOpacity:
    """
    Opacity of tractography display, [0.0, 1.0], default is 1.0.
    """
    value: float | int
    """Opacity of tractography display, [0.0, 1.0], default is 1.0."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.opacity")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyOpacityOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyOpacityOutputs`).
        """
        ret = TractographyOpacityOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographySlabOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographySlab.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographySlab:
    """
    Slab thickness of tractography display, in mm. -1 to turn off crop to slab.
    """
    value: float | int
    """Slab thickness of tractography display, in mm. -1 to turn off crop to
    slab."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.slab")
        cargs.append(str(self.value))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographySlabOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographySlabOutputs`).
        """
        ret = TractographySlabOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyLightingOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyLighting.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyLighting:
    """
    Toggle the use of lighting of tractogram geometry
    """
    value: str
    """Toggle the use of lighting of tractogram geometry"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.lighting")
        cargs.append(self.value)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyLightingOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyLightingOutputs`).
        """
        ret = TractographyLightingOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyColourOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyColour.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyColour:
    """
    Specify a manual colour for the tractogram, as three comma-separated values
    """
    r_g_b: list[float | int]
    """Specify a manual colour for the tractogram, as three comma-separated
    values"""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.colour")
        cargs.extend(map(str, self.r_g_b))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyColourOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyColourOutputs`).
        """
        ret = TractographyColourOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyTsfLoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyTsfLoad.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyTsfLoad:
    """
    Load the specified tractography scalar file.
    """
    tsf: InputPathType
    """Load the specified tractography scalar file."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.tsf_load")
        cargs.append(execution.input_file(self.tsf))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyTsfLoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyTsfLoadOutputs`).
        """
        ret = TractographyTsfLoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyTsfRangeOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyTsfRange.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyTsfRange:
    """
    Set range for the tractography scalar file. Requires -tractography.tsf_load already provided.
    """
    range_min_range_max: list[float | int]
    """Set range for the tractography scalar file. Requires
    -tractography.tsf_load already provided."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.tsf_range")
        cargs.extend(map(str, self.range_min_range_max))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyTsfRangeOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyTsfRangeOutputs`).
        """
        ret = TractographyTsfRangeOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyTsfThreshOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyTsfThresh.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyTsfThresh:
    """
    Set thresholds for the tractography scalar file. Requires -tractography.tsf_load already provided.
    """
    threshold_min_theshold_max: list[float | int]
    """Set thresholds for the tractography scalar file. Requires
    -tractography.tsf_load already provided."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.tsf_thresh")
        cargs.extend(map(str, self.threshold_min_theshold_max))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyTsfThreshOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyTsfThreshOutputs`).
        """
        ret = TractographyTsfThreshOutputs(
            root=execution.output_file("."),
        )
        return ret


class TractographyTsfColourmapOutputs(typing.NamedTuple):
    """
    Output object returned when calling `TractographyTsfColourmap.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class TractographyTsfColourmap:
    """
    Sets the colourmap of the .tsf file as indexed in the tsf colourmap dropdown menu. Requires -tractography.tsf_load already.
    """
    index: int
    """Sets the colourmap of the .tsf file as indexed in the tsf colourmap
    dropdown menu. Requires -tractography.tsf_load already."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-tractography.tsf_colourmap")
        cargs.append(str(self.index))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> TractographyTsfColourmapOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `TractographyTsfColourmapOutputs`).
        """
        ret = TractographyTsfColourmapOutputs(
            root=execution.output_file("."),
        )
        return ret


class OdfLoadShOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OdfLoadSh.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OdfLoadSh:
    """
    Loads the specified SH-based ODF image on the ODF tool.
    """
    image_: InputPathType
    """Loads the specified SH-based ODF image on the ODF tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-odf.load_sh")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OdfLoadShOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OdfLoadShOutputs`).
        """
        ret = OdfLoadShOutputs(
            root=execution.output_file("."),
        )
        return ret


class OdfLoadTensorOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OdfLoadTensor.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OdfLoadTensor:
    """
    Loads the specified tensor image on the ODF tool.
    """
    image_: InputPathType
    """Loads the specified tensor image on the ODF tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-odf.load_tensor")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OdfLoadTensorOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OdfLoadTensorOutputs`).
        """
        ret = OdfLoadTensorOutputs(
            root=execution.output_file("."),
        )
        return ret


class OdfLoadDixelOutputs(typing.NamedTuple):
    """
    Output object returned when calling `OdfLoadDixel.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class OdfLoadDixel:
    """
    Loads the specified dixel-based image on the ODF tool.
    """
    image_: InputPathType
    """Loads the specified dixel-based image on the ODF tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-odf.load_dixel")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> OdfLoadDixelOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `OdfLoadDixelOutputs`).
        """
        ret = OdfLoadDixelOutputs(
            root=execution.output_file("."),
        )
        return ret


class FixelLoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `FixelLoad.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class FixelLoad:
    """
    Load a fixel file (any file inside a fixel directory, or an old .msf / .msh legacy format file) into the fixel tool.
    """
    image_: InputPathType
    """Load a fixel file (any file inside a fixel directory, or an old .msf /
    .msh legacy format file) into the fixel tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-fixel.load")
        cargs.append(execution.input_file(self.image_))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> FixelLoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `FixelLoadOutputs`).
        """
        ret = FixelLoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class ConnectomeLoadOutputs(typing.NamedTuple):
    """
    Output object returned when calling `ConnectomeLoad.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class ConnectomeLoad:
    """
    Load a matrix file into the connectome tool.
    """
    path: InputPathType
    """Load a matrix file into the connectome tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-connectome.load")
        cargs.append(execution.input_file(self.path))
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ConnectomeLoadOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ConnectomeLoadOutputs`).
        """
        ret = ConnectomeLoadOutputs(
            root=execution.output_file("."),
        )
        return ret


class CaptureFolderOutputs(typing.NamedTuple):
    """
    Output object returned when calling `CaptureFolder.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class CaptureFolder:
    """
    Set the output folder for the screen capture tool.
    """
    path: str
    """Set the output folder for the screen capture tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-capture.folder")
        cargs.append(self.path)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> CaptureFolderOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `CaptureFolderOutputs`).
        """
        ret = CaptureFolderOutputs(
            root=execution.output_file("."),
        )
        return ret


class CapturePrefixOutputs(typing.NamedTuple):
    """
    Output object returned when calling `CapturePrefix.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class CapturePrefix:
    """
    Set the output file prefix for the screen capture tool.
    """
    string_: str
    """Set the output file prefix for the screen capture tool."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-capture.prefix")
        cargs.append(self.string_)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> CapturePrefixOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `CapturePrefixOutputs`).
        """
        ret = CapturePrefixOutputs(
            root=execution.output_file("."),
        )
        return ret


class CaptureGrabOutputs(typing.NamedTuple):
    """
    Output object returned when calling `CaptureGrab.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class CaptureGrab:
    """
    Start the screen capture process.
    """
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-capture.grab")
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> CaptureGrabOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `CaptureGrabOutputs`).
        """
        ret = CaptureGrabOutputs(
            root=execution.output_file("."),
        )
        return ret


class ConfigOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Config.run(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


@dataclasses.dataclass
class Config:
    """
    temporarily set the value of an MRtrix config file entry.
    """
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""
    
    def run(
        self,
        execution: Execution,
    ) -> list[str]:
        """
        Build command line arguments. This method is called by the main command.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            
        """
        cargs = []
        cargs.append("-config")
        cargs.append(self.key)
        cargs.append(self.value)
        return cargs
    
    def outputs(
        self,
        execution: Execution,
    ) -> ConfigOutputs:
        """
        Collect output file paths.
        
        Args:
            self: The sub-command object.
            execution: The execution object.
        Returns:
            NamedTuple of outputs (described in `ConfigOutputs`).
        """
        ret = ConfigOutputs(
            root=execution.output_file("."),
        )
        return ret


class MrviewOutputs(typing.NamedTuple):
    """
    Output object returned when calling `mrview(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    mode: ModeOutputs
    """Subcommand outputs"""
    load: LoadOutputs
    """Subcommand outputs"""
    reset: ResetOutputs
    """Subcommand outputs"""
    fov: FovOutputs
    """Subcommand outputs"""
    focus: FocusOutputs
    """Subcommand outputs"""
    target: TargetOutputs
    """Subcommand outputs"""
    voxel: VoxelOutputs
    """Subcommand outputs"""
    volume: VolumeOutputs
    """Subcommand outputs"""
    plane: PlaneOutputs
    """Subcommand outputs"""
    lock: LockOutputs
    """Subcommand outputs"""
    select_image: SelectImageOutputs
    """Subcommand outputs"""
    autoscale: AutoscaleOutputs
    """Subcommand outputs"""
    interpolation: InterpolationOutputs
    """Subcommand outputs"""
    colourmap: ColourmapOutputs
    """Subcommand outputs"""
    noannotations: NoannotationsOutputs
    """Subcommand outputs"""
    comments: CommentsOutputs
    """Subcommand outputs"""
    voxelinfo: VoxelinfoOutputs
    """Subcommand outputs"""
    orientationlabel: OrientationlabelOutputs
    """Subcommand outputs"""
    colourbar: ColourbarOutputs
    """Subcommand outputs"""
    imagevisible: ImagevisibleOutputs
    """Subcommand outputs"""
    intensity_range: IntensityRangeOutputs
    """Subcommand outputs"""
    size: SizeOutputs
    """Subcommand outputs"""
    position: PositionOutputs
    """Subcommand outputs"""
    overlay_load: OverlayLoadOutputs
    """Subcommand outputs"""
    overlay_opacity: OverlayOpacityOutputs
    """Subcommand outputs"""
    overlay_colourmap: OverlayColourmapOutputs
    """Subcommand outputs"""
    overlay_colour: OverlayColourOutputs
    """Subcommand outputs"""
    overlay_intensity: OverlayIntensityOutputs
    """Subcommand outputs"""
    overlay_threshold_min: OverlayThresholdMinOutputs
    """Subcommand outputs"""
    overlay_threshold_max: OverlayThresholdMaxOutputs
    """Subcommand outputs"""
    overlay_no_threshold_min: OverlayNoThresholdMinOutputs
    """Subcommand outputs"""
    overlay_no_threshold_max: OverlayNoThresholdMaxOutputs
    """Subcommand outputs"""
    overlay_interpolation: OverlayInterpolationOutputs
    """Subcommand outputs"""
    roi_load: RoiLoadOutputs
    """Subcommand outputs"""
    roi_opacity: RoiOpacityOutputs
    """Subcommand outputs"""
    roi_colour: RoiColourOutputs
    """Subcommand outputs"""
    tractography_load: TractographyLoadOutputs
    """Subcommand outputs"""
    tractography_thickness: TractographyThicknessOutputs
    """Subcommand outputs"""
    tractography_geometry: TractographyGeometryOutputs
    """Subcommand outputs"""
    tractography_opacity: TractographyOpacityOutputs
    """Subcommand outputs"""
    tractography_slab: TractographySlabOutputs
    """Subcommand outputs"""
    tractography_lighting: TractographyLightingOutputs
    """Subcommand outputs"""
    tractography_colour: TractographyColourOutputs
    """Subcommand outputs"""
    tractography_tsf_load: TractographyTsfLoadOutputs
    """Subcommand outputs"""
    tractography_tsf_range: TractographyTsfRangeOutputs
    """Subcommand outputs"""
    tractography_tsf_thresh: TractographyTsfThreshOutputs
    """Subcommand outputs"""
    tractography_tsf_colourmap: TractographyTsfColourmapOutputs
    """Subcommand outputs"""
    odf_load_sh: OdfLoadShOutputs
    """Subcommand outputs"""
    odf_load_tensor: OdfLoadTensorOutputs
    """Subcommand outputs"""
    odf_load_dixel: OdfLoadDixelOutputs
    """Subcommand outputs"""
    fixel_load: FixelLoadOutputs
    """Subcommand outputs"""
    connectome_load: ConnectomeLoadOutputs
    """Subcommand outputs"""
    capture_folder: CaptureFolderOutputs
    """Subcommand outputs"""
    capture_prefix: CapturePrefixOutputs
    """Subcommand outputs"""
    capture_grab: CaptureGrabOutputs
    """Subcommand outputs"""
    config: ConfigOutputs
    """Subcommand outputs"""


def mrview(
    image: list[InputPathType] = None,
    mode: list[Mode] = None,
    load: list[Load] = None,
    reset: list[Reset] = None,
    fov: list[Fov] = None,
    focus: list[Focus] = None,
    target: list[Target] = None,
    voxel: list[Voxel] = None,
    volume: list[Volume] = None,
    plane: list[Plane] = None,
    lock: list[Lock] = None,
    select_image: list[SelectImage] = None,
    autoscale: list[Autoscale] = None,
    interpolation: list[Interpolation] = None,
    colourmap: list[Colourmap] = None,
    noannotations: list[Noannotations] = None,
    comments: list[Comments] = None,
    voxelinfo: list[Voxelinfo] = None,
    orientationlabel: list[Orientationlabel] = None,
    colourbar: list[Colourbar] = None,
    imagevisible: list[Imagevisible] = None,
    intensity_range: list[IntensityRange] = None,
    size: list[Size] = None,
    position: list[Position] = None,
    fullscreen: bool = False,
    exit_: bool = False,
    sync_focus: bool = False,
    fps: bool = False,
    overlay_load: list[OverlayLoad] = None,
    overlay_opacity: list[OverlayOpacity] = None,
    overlay_colourmap: list[OverlayColourmap] = None,
    overlay_colour: list[OverlayColour] = None,
    overlay_intensity: list[OverlayIntensity] = None,
    overlay_threshold_min: list[OverlayThresholdMin] = None,
    overlay_threshold_max: list[OverlayThresholdMax] = None,
    overlay_no_threshold_min: list[OverlayNoThresholdMin] = None,
    overlay_no_threshold_max: list[OverlayNoThresholdMax] = None,
    overlay_interpolation: list[OverlayInterpolation] = None,
    roi_load: list[RoiLoad] = None,
    roi_opacity: list[RoiOpacity] = None,
    roi_colour: list[RoiColour] = None,
    tractography_load: list[TractographyLoad] = None,
    tractography_thickness: list[TractographyThickness] = None,
    tractography_geometry: list[TractographyGeometry] = None,
    tractography_opacity: list[TractographyOpacity] = None,
    tractography_slab: list[TractographySlab] = None,
    tractography_lighting: list[TractographyLighting] = None,
    tractography_colour: list[TractographyColour] = None,
    tractography_tsf_load: list[TractographyTsfLoad] = None,
    tractography_tsf_range: list[TractographyTsfRange] = None,
    tractography_tsf_thresh: list[TractographyTsfThresh] = None,
    tractography_tsf_colourmap: list[TractographyTsfColourmap] = None,
    odf_load_sh: list[OdfLoadSh] = None,
    odf_load_tensor: list[OdfLoadTensor] = None,
    odf_load_dixel: list[OdfLoadDixel] = None,
    fixel_load: list[FixelLoad] = None,
    connectome_init: InputPathType | None = None,
    connectome_load: list[ConnectomeLoad] = None,
    capture_folder: list[CaptureFolder] = None,
    capture_prefix: list[CapturePrefix] = None,
    capture_grab: list[CaptureGrab] = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Config] = None,
    help_: bool = False,
    version: bool = False,
    runner: Runner = None,
) -> MrviewOutputs:
    """
    mrview by J-Donald Tournier (jdtournier@gmail.com), Dave Raffelt
    (david.raffelt@florey.edu.au), Robert E. Smith (robert.smith@florey.edu.au),
    Rami Tabbara (rami.tabbara@florey.edu.au), Max Pietsch
    (maximilian.pietsch@kcl.ac.uk), Thijs Dhollander (thijs.dhollander@gmail.com).
    
    The MRtrix image viewer.
    
    Any images listed as arguments will be loaded and available through the
    image menu, with the first listed displayed initially. Any subsequent
    command-line options will be processed as if the corresponding action had
    been performed through the GUI.
    
    Note that because images loaded as arguments (i.e. simply listed on the
    command-line) are opened before the GUI is shown, subsequent actions to be
    performed via the various command-line options must appear after the last
    argument. This is to avoid confusion about which option will apply to which
    image. If you need fine control over this, please use the -load or
    -select_image options. For example:
    
    $ mrview -load image1.mif -interpolation 0 -load image2.mif -interpolation 0
    
    or
    
    $ mrview image1.mif image2.mif -interpolation 0 -select_image 2
    -interpolation 0
    
    References:
    
    Tournier, J.-D.; Calamante, F. & Connelly, A. MRtrix: Diffusion tractography
    in crossing fiber regions. Int. J. Imaging Syst. Technol., 2012, 22, 53-66.
    
    More information:
    https://mrtrix.readthedocs.io/en/latest/reference/commands/mrview.html
    
    Args:
        image: An image to be loaded.
        mode: Switch to view mode specified by the integer index, as per the
            view menu.
        load: Load image specified and make it current.
        reset: Reset the view according to current image. This resets the FOV,
            projection and focus.
        fov: Set the field of view, in mm.
        focus: Either set the position of the crosshairs in scanner coordinates,
            with the new position supplied as a comma-separated list of
            floating-point values or show or hide the focus cross hair using a
            boolean value as argument.
        target: Set the target location for the viewing window (the scanner
            coordinate that will appear at the centre of the viewing window
        voxel: Set the position of the crosshairs in voxel coordinates, relative
            the image currently displayed. The new position should be supplied as a
            comma-separated list of floating-point values.
        volume: Set the volume index for the image displayed, as a
            comma-separated list of integers.
        plane: Set the viewing plane, according to the mappping 0: sagittal; 1:
            coronal; 2: axial.
        lock: Set whether view is locked to image axes (0: no, 1: yes).
        select_image: Switch to image number specified, with reference to the
            list of currently loaded images.
        autoscale: Reset the image scaling to automatically determined range.
        interpolation: Enable or disable image interpolation in main image.
        colourmap: Switch the image colourmap to that specified, as per the
            colourmap menu.
        noannotations: Hide all image annotation overlays
        comments: Show or hide image comments overlay.
        voxelinfo: Show or hide voxel information overlay.
        orientationlabel: Show or hide orientation label overlay.
        colourbar: Show or hide colourbar overlay.
        imagevisible: Show or hide the main image.
        intensity_range: Set the image intensity range to that specified.
        size: Set the size of the view area, in pixel units.
        position: Set the position of the main window, in pixel units.
        fullscreen: Start fullscreen.
        exit_: Quit MRView.
        sync_focus: Sync the focus with other MRView windows that also have this
            turned on.
        fps: Display frames per second, averaged over the last 10 frames. The
            maximum over the last 3 seconds is also displayed.
        overlay_load: Loads the specified image on the overlay tool.
        overlay_opacity: Sets the overlay opacity to floating value [0-1].
        overlay_colourmap: Sets the colourmap of the overlay as indexed in the
            colourmap dropdown menu.
        overlay_colour: Specify a manual colour for the overlay, as three
            comma-separated values
        overlay_intensity: Set the intensity windowing of the overlay
        overlay_threshold_min: Set the lower threshold value of the overlay
        overlay_threshold_max: Set the upper threshold value of the overlay
        overlay_no_threshold_min: Disable the lower threshold for the overlay
        overlay_no_threshold_max: Disable the upper threshold for the overlay
        overlay_interpolation: Enable or disable overlay image interpolation.
        roi_load: Loads the specified image on the ROI editor tool.
        roi_opacity: Sets the overlay opacity to floating value [0-1].
        roi_colour: Sets the colour of the ROI overlay
        tractography_load: Load the specified tracks file into the tractography
            tool.
        tractography_thickness: Line thickness of tractography display, [-1.0,
            1.0], default is 0.0.
        tractography_geometry: The geometry type to use when rendering
            tractograms (options are: pseudotubes, lines, points)
        tractography_opacity: Opacity of tractography display, [0.0, 1.0],
            default is 1.0.
        tractography_slab: Slab thickness of tractography display, in mm. -1 to
            turn off crop to slab.
        tractography_lighting: Toggle the use of lighting of tractogram geometry
        tractography_colour: Specify a manual colour for the tractogram, as
            three comma-separated values
        tractography_tsf_load: Load the specified tractography scalar file.
        tractography_tsf_range: Set range for the tractography scalar file.
            Requires -tractography.tsf_load already provided.
        tractography_tsf_thresh: Set thresholds for the tractography scalar
            file. Requires -tractography.tsf_load already provided.
        tractography_tsf_colourmap: Sets the colourmap of the .tsf file as
            indexed in the tsf colourmap dropdown menu. Requires
            -tractography.tsf_load already.
        odf_load_sh: Loads the specified SH-based ODF image on the ODF tool.
        odf_load_tensor: Loads the specified tensor image on the ODF tool.
        odf_load_dixel: Loads the specified dixel-based image on the ODF tool.
        fixel_load: Load a fixel file (any file inside a fixel directory, or an
            old .msf / .msh legacy format file) into the fixel tool.
        connectome_init: Initialise the connectome tool using a parcellation
            image.
        connectome_load: Load a matrix file into the connectome tool.
        capture_folder: Set the output folder for the screen capture tool.
        capture_prefix: Set the output file prefix for the screen capture tool.
        capture_grab: Start the screen capture process.
        info: display information messages.
        quiet: do not display information messages or progress status;
            alternatively, this can be achieved by setting the MRTRIX_QUIET
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set
            to 0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help_: display this information page and exit.
        version: display version information and exit.
        runner: Command runner
    Returns:
        NamedTuple of outputs (described in `MrviewOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(MRVIEW_METADATA)
    cargs = []
    cargs.append("mrview")
    if mode is not None:
        cargs.extend([a for c in [s.run(execution) for s in mode] for a in c])
    if load is not None:
        cargs.extend([a for c in [s.run(execution) for s in load] for a in c])
    if reset is not None:
        cargs.extend([a for c in [s.run(execution) for s in reset] for a in c])
    if fov is not None:
        cargs.extend([a for c in [s.run(execution) for s in fov] for a in c])
    if focus is not None:
        cargs.extend([a for c in [s.run(execution) for s in focus] for a in c])
    if target is not None:
        cargs.extend([a for c in [s.run(execution) for s in target] for a in c])
    if voxel is not None:
        cargs.extend([a for c in [s.run(execution) for s in voxel] for a in c])
    if volume is not None:
        cargs.extend([a for c in [s.run(execution) for s in volume] for a in c])
    if plane is not None:
        cargs.extend([a for c in [s.run(execution) for s in plane] for a in c])
    if lock is not None:
        cargs.extend([a for c in [s.run(execution) for s in lock] for a in c])
    if select_image is not None:
        cargs.extend([a for c in [s.run(execution) for s in select_image] for a in c])
    if autoscale is not None:
        cargs.extend([a for c in [s.run(execution) for s in autoscale] for a in c])
    if interpolation is not None:
        cargs.extend([a for c in [s.run(execution) for s in interpolation] for a in c])
    if colourmap is not None:
        cargs.extend([a for c in [s.run(execution) for s in colourmap] for a in c])
    if noannotations is not None:
        cargs.extend([a for c in [s.run(execution) for s in noannotations] for a in c])
    if comments is not None:
        cargs.extend([a for c in [s.run(execution) for s in comments] for a in c])
    if voxelinfo is not None:
        cargs.extend([a for c in [s.run(execution) for s in voxelinfo] for a in c])
    if orientationlabel is not None:
        cargs.extend([a for c in [s.run(execution) for s in orientationlabel] for a in c])
    if colourbar is not None:
        cargs.extend([a for c in [s.run(execution) for s in colourbar] for a in c])
    if imagevisible is not None:
        cargs.extend([a for c in [s.run(execution) for s in imagevisible] for a in c])
    if intensity_range is not None:
        cargs.extend([a for c in [s.run(execution) for s in intensity_range] for a in c])
    if size is not None:
        cargs.extend([a for c in [s.run(execution) for s in size] for a in c])
    if position is not None:
        cargs.extend([a for c in [s.run(execution) for s in position] for a in c])
    if fullscreen:
        cargs.append("-fullscreen")
    if exit_:
        cargs.append("-exit")
    if sync_focus:
        cargs.append("-sync.focus")
    if fps:
        cargs.append("-fps")
    if overlay_load is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_load] for a in c])
    if overlay_opacity is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_opacity] for a in c])
    if overlay_colourmap is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_colourmap] for a in c])
    if overlay_colour is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_colour] for a in c])
    if overlay_intensity is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_intensity] for a in c])
    if overlay_threshold_min is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_threshold_min] for a in c])
    if overlay_threshold_max is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_threshold_max] for a in c])
    if overlay_no_threshold_min is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_no_threshold_min] for a in c])
    if overlay_no_threshold_max is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_no_threshold_max] for a in c])
    if overlay_interpolation is not None:
        cargs.extend([a for c in [s.run(execution) for s in overlay_interpolation] for a in c])
    if roi_load is not None:
        cargs.extend([a for c in [s.run(execution) for s in roi_load] for a in c])
    if roi_opacity is not None:
        cargs.extend([a for c in [s.run(execution) for s in roi_opacity] for a in c])
    if roi_colour is not None:
        cargs.extend([a for c in [s.run(execution) for s in roi_colour] for a in c])
    if tractography_load is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_load] for a in c])
    if tractography_thickness is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_thickness] for a in c])
    if tractography_geometry is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_geometry] for a in c])
    if tractography_opacity is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_opacity] for a in c])
    if tractography_slab is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_slab] for a in c])
    if tractography_lighting is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_lighting] for a in c])
    if tractography_colour is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_colour] for a in c])
    if tractography_tsf_load is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_tsf_load] for a in c])
    if tractography_tsf_range is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_tsf_range] for a in c])
    if tractography_tsf_thresh is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_tsf_thresh] for a in c])
    if tractography_tsf_colourmap is not None:
        cargs.extend([a for c in [s.run(execution) for s in tractography_tsf_colourmap] for a in c])
    if odf_load_sh is not None:
        cargs.extend([a for c in [s.run(execution) for s in odf_load_sh] for a in c])
    if odf_load_tensor is not None:
        cargs.extend([a for c in [s.run(execution) for s in odf_load_tensor] for a in c])
    if odf_load_dixel is not None:
        cargs.extend([a for c in [s.run(execution) for s in odf_load_dixel] for a in c])
    if fixel_load is not None:
        cargs.extend([a for c in [s.run(execution) for s in fixel_load] for a in c])
    if connectome_init is not None:
        cargs.extend(["-connectome.init", execution.input_file(connectome_init)])
    if connectome_load is not None:
        cargs.extend([a for c in [s.run(execution) for s in connectome_load] for a in c])
    if capture_folder is not None:
        cargs.extend([a for c in [s.run(execution) for s in capture_folder] for a in c])
    if capture_prefix is not None:
        cargs.extend([a for c in [s.run(execution) for s in capture_prefix] for a in c])
    if capture_grab is not None:
        cargs.extend([a for c in [s.run(execution) for s in capture_grab] for a in c])
    if info:
        cargs.append("-info")
    if quiet:
        cargs.append("-quiet")
    if debug:
        cargs.append("-debug")
    if force:
        cargs.append("-force")
    if nthreads is not None:
        cargs.extend(["-nthreads", str(nthreads)])
    if config is not None:
        cargs.extend([a for c in [s.run(execution) for s in config] for a in c])
    if help_:
        cargs.append("-help")
    if version:
        cargs.append("-version")
    if image is not None:
        cargs.extend([execution.input_file(f) for f in image])
    ret = MrviewOutputs(
        root=execution.output_file("."),
        mode=[mode.outputs(execution) for mode in mode],
        load=[load.outputs(execution) for load in load],
        reset=[reset.outputs(execution) for reset in reset],
        fov=[fov.outputs(execution) for fov in fov],
        focus=[focus.outputs(execution) for focus in focus],
        target=[target.outputs(execution) for target in target],
        voxel=[voxel.outputs(execution) for voxel in voxel],
        volume=[volume.outputs(execution) for volume in volume],
        plane=[plane.outputs(execution) for plane in plane],
        lock=[lock.outputs(execution) for lock in lock],
        select_image=[select_image.outputs(execution) for select_image in select_image],
        autoscale=[autoscale.outputs(execution) for autoscale in autoscale],
        interpolation=[interpolation.outputs(execution) for interpolation in interpolation],
        colourmap=[colourmap.outputs(execution) for colourmap in colourmap],
        noannotations=[noannotations.outputs(execution) for noannotations in noannotations],
        comments=[comments.outputs(execution) for comments in comments],
        voxelinfo=[voxelinfo.outputs(execution) for voxelinfo in voxelinfo],
        orientationlabel=[orientationlabel.outputs(execution) for orientationlabel in orientationlabel],
        colourbar=[colourbar.outputs(execution) for colourbar in colourbar],
        imagevisible=[imagevisible.outputs(execution) for imagevisible in imagevisible],
        intensity_range=[intensity_range.outputs(execution) for intensity_range in intensity_range],
        size=[size.outputs(execution) for size in size],
        position=[position.outputs(execution) for position in position],
        overlay_load=[overlay_load.outputs(execution) for overlay_load in overlay_load],
        overlay_opacity=[overlay_opacity.outputs(execution) for overlay_opacity in overlay_opacity],
        overlay_colourmap=[overlay_colourmap.outputs(execution) for overlay_colourmap in overlay_colourmap],
        overlay_colour=[overlay_colour.outputs(execution) for overlay_colour in overlay_colour],
        overlay_intensity=[overlay_intensity.outputs(execution) for overlay_intensity in overlay_intensity],
        overlay_threshold_min=[overlay_threshold_min.outputs(execution) for overlay_threshold_min in overlay_threshold_min],
        overlay_threshold_max=[overlay_threshold_max.outputs(execution) for overlay_threshold_max in overlay_threshold_max],
        overlay_no_threshold_min=[overlay_no_threshold_min.outputs(execution) for overlay_no_threshold_min in overlay_no_threshold_min],
        overlay_no_threshold_max=[overlay_no_threshold_max.outputs(execution) for overlay_no_threshold_max in overlay_no_threshold_max],
        overlay_interpolation=[overlay_interpolation.outputs(execution) for overlay_interpolation in overlay_interpolation],
        roi_load=[roi_load.outputs(execution) for roi_load in roi_load],
        roi_opacity=[roi_opacity.outputs(execution) for roi_opacity in roi_opacity],
        roi_colour=[roi_colour.outputs(execution) for roi_colour in roi_colour],
        tractography_load=[tractography_load.outputs(execution) for tractography_load in tractography_load],
        tractography_thickness=[tractography_thickness.outputs(execution) for tractography_thickness in tractography_thickness],
        tractography_geometry=[tractography_geometry.outputs(execution) for tractography_geometry in tractography_geometry],
        tractography_opacity=[tractography_opacity.outputs(execution) for tractography_opacity in tractography_opacity],
        tractography_slab=[tractography_slab.outputs(execution) for tractography_slab in tractography_slab],
        tractography_lighting=[tractography_lighting.outputs(execution) for tractography_lighting in tractography_lighting],
        tractography_colour=[tractography_colour.outputs(execution) for tractography_colour in tractography_colour],
        tractography_tsf_load=[tractography_tsf_load.outputs(execution) for tractography_tsf_load in tractography_tsf_load],
        tractography_tsf_range=[tractography_tsf_range.outputs(execution) for tractography_tsf_range in tractography_tsf_range],
        tractography_tsf_thresh=[tractography_tsf_thresh.outputs(execution) for tractography_tsf_thresh in tractography_tsf_thresh],
        tractography_tsf_colourmap=[tractography_tsf_colourmap.outputs(execution) for tractography_tsf_colourmap in tractography_tsf_colourmap],
        odf_load_sh=[odf_load_sh.outputs(execution) for odf_load_sh in odf_load_sh],
        odf_load_tensor=[odf_load_tensor.outputs(execution) for odf_load_tensor in odf_load_tensor],
        odf_load_dixel=[odf_load_dixel.outputs(execution) for odf_load_dixel in odf_load_dixel],
        fixel_load=[fixel_load.outputs(execution) for fixel_load in fixel_load],
        connectome_load=[connectome_load.outputs(execution) for connectome_load in connectome_load],
        capture_folder=[capture_folder.outputs(execution) for capture_folder in capture_folder],
        capture_prefix=[capture_prefix.outputs(execution) for capture_prefix in capture_prefix],
        capture_grab=[capture_grab.outputs(execution) for capture_grab in capture_grab],
        config=[config.outputs(execution) for config in config],
    )
    execution.run(cargs)
    return ret


__all__ = [
    "Autoscale",
    "AutoscaleOutputs",
    "CaptureFolder",
    "CaptureFolderOutputs",
    "CaptureGrab",
    "CaptureGrabOutputs",
    "CapturePrefix",
    "CapturePrefixOutputs",
    "Colourbar",
    "ColourbarOutputs",
    "Colourmap",
    "ColourmapOutputs",
    "Comments",
    "CommentsOutputs",
    "Config",
    "ConfigOutputs",
    "ConnectomeLoad",
    "ConnectomeLoadOutputs",
    "FixelLoad",
    "FixelLoadOutputs",
    "Focus",
    "FocusOutputs",
    "Fov",
    "FovOutputs",
    "Imagevisible",
    "ImagevisibleOutputs",
    "IntensityRange",
    "IntensityRangeOutputs",
    "Interpolation",
    "InterpolationOutputs",
    "Load",
    "LoadOutputs",
    "Lock",
    "LockOutputs",
    "MRVIEW_METADATA",
    "Mode",
    "ModeOutputs",
    "MrviewOutputs",
    "Noannotations",
    "NoannotationsOutputs",
    "OdfLoadDixel",
    "OdfLoadDixelOutputs",
    "OdfLoadSh",
    "OdfLoadShOutputs",
    "OdfLoadTensor",
    "OdfLoadTensorOutputs",
    "Orientationlabel",
    "OrientationlabelOutputs",
    "OverlayColour",
    "OverlayColourOutputs",
    "OverlayColourmap",
    "OverlayColourmapOutputs",
    "OverlayIntensity",
    "OverlayIntensityOutputs",
    "OverlayInterpolation",
    "OverlayInterpolationOutputs",
    "OverlayLoad",
    "OverlayLoadOutputs",
    "OverlayNoThresholdMax",
    "OverlayNoThresholdMaxOutputs",
    "OverlayNoThresholdMin",
    "OverlayNoThresholdMinOutputs",
    "OverlayOpacity",
    "OverlayOpacityOutputs",
    "OverlayThresholdMax",
    "OverlayThresholdMaxOutputs",
    "OverlayThresholdMin",
    "OverlayThresholdMinOutputs",
    "Plane",
    "PlaneOutputs",
    "Position",
    "PositionOutputs",
    "Reset",
    "ResetOutputs",
    "RoiColour",
    "RoiColourOutputs",
    "RoiLoad",
    "RoiLoadOutputs",
    "RoiOpacity",
    "RoiOpacityOutputs",
    "SelectImage",
    "SelectImageOutputs",
    "Size",
    "SizeOutputs",
    "Target",
    "TargetOutputs",
    "TractographyColour",
    "TractographyColourOutputs",
    "TractographyGeometry",
    "TractographyGeometryOutputs",
    "TractographyLighting",
    "TractographyLightingOutputs",
    "TractographyLoad",
    "TractographyLoadOutputs",
    "TractographyOpacity",
    "TractographyOpacityOutputs",
    "TractographySlab",
    "TractographySlabOutputs",
    "TractographyThickness",
    "TractographyThicknessOutputs",
    "TractographyTsfColourmap",
    "TractographyTsfColourmapOutputs",
    "TractographyTsfLoad",
    "TractographyTsfLoadOutputs",
    "TractographyTsfRange",
    "TractographyTsfRangeOutputs",
    "TractographyTsfThresh",
    "TractographyTsfThreshOutputs",
    "Volume",
    "VolumeOutputs",
    "Voxel",
    "VoxelOutputs",
    "Voxelinfo",
    "VoxelinfoOutputs",
    "mrview",
]
