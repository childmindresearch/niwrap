# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

FROM_RAI_METADATA = Metadata(
    id="c267f878628e42907a2d26cda01b63ccba65d5be",
    name="FromRAI",
    container_image_type="docker",
    container_image_tag="fcpindi/c-pac:latest",
)


class FromRaiOutputs(typing.NamedTuple):
    """
    Output object returned when calling `from_rai(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def from_rai(
    rai_coordinates: list[float | int],
    orientation: str,
    runner: Runner | None = None,
) -> FromRaiOutputs:
    """
    FromRAI by AFNI Team.
    
    Changes the RAI coordinates to the specified orientation.
    
    More information:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/@FromRAI.html
    
    Args:
        rai_coordinates: RAI coordinates X, Y, and Z.
        orientation: Orientation format.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `FromRaiOutputs`).
    """
    runner = runner or get_global_runner()
    if (len(rai_coordinates) != 3): 
        raise ValueError(f"Length of 'rai_coordinates' must be 3 but was {len(rai_coordinates)}")
    execution = runner.start_execution(FROM_RAI_METADATA)
    cargs = []
    cargs.append("@FromRAI")
    cargs.extend(["-xyz", *map(str, rai_coordinates)])
    cargs.extend(["-or", orientation])
    ret = FromRaiOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "FROM_RAI_METADATA",
    "FromRaiOutputs",
    "from_rai",
]
