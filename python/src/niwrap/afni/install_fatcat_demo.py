# This file was auto generated by Styx.
# Do not edit this file directly.

import typing
import pathlib
from styxdefs import *
import dataclasses

INSTALL_FATCAT_DEMO_METADATA = Metadata(
    id="00c8c91106f8ca9345d8a56ae637c6407ffb3b9d.boutiques",
    name="Install_FATCAT_DEMO",
    package="afni",
    container_image_tag="afni/afni_make_build:AFNI_24.2.06",
)


class InstallFatcatDemoOutputs(typing.NamedTuple):
    """
    Output object returned when calling `install_fatcat_demo(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def install_fatcat_demo(
    wget: bool = False,
    curl: bool = False,
    runner: Runner | None = None,
) -> InstallFatcatDemoOutputs:
    """
    Installs the demo archive for Paul Taylor's tractography tools.
    
    Author: AFNI Developers
    
    URL: https://afni.nimh.nih.gov/
    
    Args:
        wget: Use wget to download archive. Script chooses by default with\
            preference for curl.
        curl: Use curl to download archive. Script chooses by default with\
            preference for curl.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `InstallFatcatDemoOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(INSTALL_FATCAT_DEMO_METADATA)
    cargs = []
    cargs.append("@Install_FATCAT_DEMO")
    if wget:
        cargs.append("-wget")
    if curl:
        cargs.append("-curl")
    ret = InstallFatcatDemoOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "INSTALL_FATCAT_DEMO_METADATA",
    "InstallFatcatDemoOutputs",
    "install_fatcat_demo",
]
