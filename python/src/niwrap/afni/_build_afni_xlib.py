# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_BUILD_AFNI_XLIB_METADATA = Metadata(
    id="989c6bea230976e79dc13656c5fcd5b6c32f0965",
    name="@build_afni_Xlib",
)


class BuildAfniXlibOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_build_afni_xlib(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def _build_afni_xlib(
    packages: list[str],
    afni_x: bool = False,
    debug_symbols: bool = False,
    lib32: bool = False,
    lib64: bool = False,
    localinstall: bool = False,
    runner: Runner | None = None,
) -> BuildAfniXlibOutputs:
    """
    @build_afni_Xlib by AFNI Team.
    
    Compile and install lesstif, openmotif, and/or libXt.
    
    Args:
        packages: Packages to compile and install (e.g., lesstif, openmotif,\
            libXt).
        afni_x: Install under /usr/local/afniX.
        debug_symbols: Compile with -g to add symbols.
        lib32: Install libs under lib, and force 32-bit compile (on Linux: add\
            --target=i386).
        lib64: Install libs under lib64 (default is lib).
        localinstall: Install under each package directory.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `BuildAfniXlibOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_BUILD_AFNI_XLIB_METADATA)
    cargs = []
    cargs.append("@build_afni_Xlib")
    if localinstall:
        cargs.append("-localinstall")
    if debug_symbols:
        cargs.append("-g")
    if lib64:
        cargs.append("-lib64")
    cargs.extend(packages)
    ret = BuildAfniXlibOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "BuildAfniXlibOutputs",
    "_BUILD_AFNI_XLIB_METADATA",
    "_build_afni_xlib",
]
