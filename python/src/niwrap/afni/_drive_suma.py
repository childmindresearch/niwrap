# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_DRIVE_SUMA_METADATA = Metadata(
    id="fa83ce72fe64edf9cb1735c64c39a808c9f792be",
    name="@DriveSuma",
    container_image_type="docker",
    container_image_index="index.docker.io",
    container_image_tag="example/image:latest",
)


class DriveSumaOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_drive_suma(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def _drive_suma(
    h_web: bool = False,
    hweb: bool = False,
    h_view: bool = False,
    hview: bool = False,
    all_opts: bool = False,
    h_find: str | None = None,
    h_raw: bool = False,
    h_txt: bool = False,
    h_spx: bool = False,
    h_aspx: bool = False,
    runner: Runner | None = None,
) -> DriveSumaOutputs:
    """
    @DriveSuma by Ziad S. Saad.
    
    A script to demonstrate how to drive suma from the command line.
    
    More information: https://discuss.afni.nimh.nih.gov/
    
    Args:
        h_web: Open webpage with help for this program.
        hweb: Same as -h_web.
        h_view: Open -help output in a GUI editor.
        hview: Same as -h_view.
        all_opts: List all of the options for this script.
        h_find: Search for lines containing WORD in -help output. Search is\
            approximate.
        h_raw: Raw help string.
        h_txt: Text formatted help string.
        h_spx: Sphinx formatted help string.
        h_aspx: Sphinx formatted help string with auto tagging of options.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `DriveSumaOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_DRIVE_SUMA_METADATA)
    cargs = []
    cargs.append("@DriveSuma")
    if h_aspx:
        cargs.append("-h_aspx")
    ret = DriveSumaOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "DriveSumaOutputs",
    "_DRIVE_SUMA_METADATA",
    "_drive_suma",
]
