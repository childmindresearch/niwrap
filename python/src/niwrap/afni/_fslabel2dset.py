# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_FSLABEL2DSET_METADATA = Metadata(
    id="8456c935d6aa68a380d3d63cd9e63252b98daa22",
    name="@FSlabel2dset",
    container_image_type="docker",
    container_image_tag="fcpindi/c-pac:latest",
)


class Fslabel2dsetOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_fslabel2dset(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def _fslabel2dset(
    fs_label_file: InputPathType,
    val: float | int | None = None,
    help_: bool = False,
    echo: bool = False,
    keep_tmp: bool = False,
    runner: Runner | None = None,
) -> Fslabel2dsetOutputs:
    """
    @FSlabel2dset by AFNI Team.
    
    A script to convert a FreeSurfer ASCII label file into a SUMA dataset and a
    SUMA ROI.
    
    More information:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/@FSlabel2dset.html
    
    Args:
        fs_label_file: Specify the ASCII label file from FreeSurfer.
        val: Assign integer VAL to the nodes in FS_LABEL_FILE (Default is 1).
        help_: Display help message.
        echo: Turn echo for debugging.
        keep_tmp: Don't cleanup temp files.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `Fslabel2dsetOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_FSLABEL2DSET_METADATA)
    cargs = []
    cargs.append("@FSlabel2dset")
    cargs.append("-fs")
    cargs.extend(["-fs", execution.input_file(fs_label_file)])
    cargs.append("-val")
    if val is not None:
        cargs.extend(["-val", str(val)])
    if help_:
        cargs.append("-help")
    if echo:
        cargs.append("-echo")
    if keep_tmp:
        cargs.append("-keep_tmp")
    ret = Fslabel2dsetOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "Fslabel2dsetOutputs",
    "_FSLABEL2DSET_METADATA",
    "_fslabel2dset",
]
