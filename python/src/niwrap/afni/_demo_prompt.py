# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_DEMO_PROMPT_METADATA = Metadata(
    id="e5a10e50755fc485f577306751bee4172cd63e06",
    name="@demo_prompt",
)


class DemoPromptOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_demo_prompt(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    status: OutputPathType
    """Status output: 0 if user presses OK, 1 if user cancels"""


def _demo_prompt(
    message: str,
    runner: Runner | None = None,
) -> DemoPromptOutputs:
    """
    @demo_prompt by Unknown.
    
    Prompts user with a message and waits for acknowledgment.
    
    Args:
        message: The message to display in the prompt.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `DemoPromptOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_DEMO_PROMPT_METADATA)
    cargs = []
    cargs.append("@demo_prompt")
    cargs.append(message)
    ret = DemoPromptOutputs(
        root=execution.output_file("."),
        status=execution.output_file(f"status"),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "DemoPromptOutputs",
    "_DEMO_PROMPT_METADATA",
    "_demo_prompt",
]
