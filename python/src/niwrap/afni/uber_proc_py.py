# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

UBER_PROC_METADATA = Metadata(
    id="d67103a5c3d7459858ce08975451fe12aea6e863",
    name="uber_proc",
    container_image_type="docker",
    container_image_index="index.docker.io",
    container_image_tag="mydockerhub/uber_proc:latest",
)


class UberProcOutputs(typing.NamedTuple):
    """
    Output object returned when calling `uber_proc(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def uber_proc(
    results_dir: str | None = None,
    runner: Runner | None = None,
) -> UberProcOutputs:
    """
    uber_proc by Anonymous.
    
    Uber processing tool - work in progress.
    
    More information: https://example.com/uber_proc
    
    Args:
        results_dir: Directory where results will be placed. Default is a new\
            'uber_results' directory in the current working directory.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `UberProcOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(UBER_PROC_METADATA)
    cargs = []
    cargs.append("uber_proc.py")
    if results_dir is not None:
        cargs.append(results_dir)
    ret = UberProcOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "UBER_PROC_METADATA",
    "UberProcOutputs",
    "uber_proc",
]
