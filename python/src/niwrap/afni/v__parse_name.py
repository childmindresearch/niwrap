# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

PARSE_NAME_METADATA = Metadata(
    id="fec643048eb67396904f612fcc609c4629cf2b6c",
    name="parse_name",
    container_image_type="docker",
    container_image_tag="fcpindi/c-pac:latest",
)


class ParseNameOutputs(typing.NamedTuple):
    """
    Output object returned when calling `parse_name(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    output_path: OutputPathType
    """The path of the filename"""
    output_prefix: OutputPathType
    """The prefix of the filename"""
    output_extension: OutputPathType
    """The extension of the filename"""


def parse_name(
    name: str,
    runner: Runner | None = None,
) -> ParseNameOutputs:
    """
    parse_name by AFNI Team.
    
    A script to parse a filename into path, prefix, and extension strings.
    
    More information:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/@parse_name.html
    
    Args:
        name: The filename to parse.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `ParseNameOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(PARSE_NAME_METADATA)
    cargs = []
    cargs.append("@parse_name")
    cargs.append(name)
    ret = ParseNameOutputs(
        root=execution.output_file("."),
        output_path=execution.output_file(f"{name}_path", optional=True),
        output_prefix=execution.output_file(f"{name}_prefix", optional=True),
        output_extension=execution.output_file(f"{name}_extension", optional=True),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "PARSE_NAME_METADATA",
    "ParseNameOutputs",
    "parse_name",
]
