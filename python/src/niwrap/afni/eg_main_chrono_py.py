# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

EG_MAIN_CHRONO_METADATA = Metadata(
    id="83729ca4ed0f857257382aa2c87c873d59b9b177",
    name="eg_main_chrono",
    container_image_type="docker",
    container_image_index="index.docker.io",
    container_image_tag="python:latest",
)


class EgMainChronoOutputs(typing.NamedTuple):
    """
    Output object returned when calling `eg_main_chrono(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def eg_main_chrono(
    help_: bool = False,
    history: bool = False,
    show_valid_options: bool = False,
    version: bool = False,
    verbosity_level: str | None = None,
    runner: Runner | None = None,
) -> EgMainChronoOutputs:
    """
    eg_main_chrono by R Reynolds.
    
    A sample main Python program to run on top of some library.
    
    Args:
        help_: Show help message.
        history: Show module history.
        show_valid_options: List valid options.
        version: Show current version.
        verbosity_level: Set the verbosity level.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `EgMainChronoOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(EG_MAIN_CHRONO_METADATA)
    cargs = []
    cargs.append("eg_main_chrono.py")
    if help_:
        cargs.append("-help")
    if history:
        cargs.append("-hist")
    if show_valid_options:
        cargs.append("-show_valid_opts")
    if version:
        cargs.append("-ver")
    if verbosity_level is not None:
        cargs.extend(["-verb", verbosity_level])
    ret = EgMainChronoOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "EG_MAIN_CHRONO_METADATA",
    "EgMainChronoOutputs",
    "eg_main_chrono",
]
