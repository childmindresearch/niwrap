# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

NP_METADATA = Metadata(
    id="b799d11cf166eb3977c6aecb35d8226d8472ded1",
    name="np",
)


class NpOutputs(typing.NamedTuple):
    """
    Output object returned when calling `np(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    outfile: OutputPathType
    """Output text file with the appropriate new prefix."""


def np(
    prefix: str,
    runner: Runner | None = None,
) -> NpOutputs:
    """
    np.
    
    Finds an appropriate new prefix to use, given the files you already have in
    your directory. It automatically creates a valid prefix when you are
    repeatedly running similar commands but do not want to delete previous
    output.
    
    Args:
        prefix: The prefix to be checked.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `NpOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(NP_METADATA)
    cargs = []
    cargs.append("@np")
    cargs.append(prefix)
    ret = NpOutputs(
        root=execution.output_file("."),
        outfile=execution.output_file(f"appropriate_prefix.txt"),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "NP_METADATA",
    "NpOutputs",
    "np",
]
