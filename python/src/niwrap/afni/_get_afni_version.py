# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_GET_AFNI_VERSION_METADATA = Metadata(
    id="dc2f253b4426c36898a7ad72e9c3267097daeeb0",
    name="@get.afni.version",
    container_image_type="docker",
    container_image_index="index.docker.io",
    container_image_tag="mcin/docker-afni:latest",
)


class GetAfniVersionOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_get_afni_version(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    src_dir: OutputPathType
    """Directory containing the downloaded AFNI source code for the specified version."""


def _get_afni_version(
    version: str,
    runner: Runner | None = None,
) -> GetAfniVersionOutputs:
    """
    @get.afni.version by RWCox.
    
    Downloads the source code for a specified AFNI version.
    
    More information: https://github.com/afni/AFNI
    
    Args:
        version: AFNI version number to get (e.g., 16.0.01).
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `GetAfniVersionOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_GET_AFNI_VERSION_METADATA)
    cargs = []
    cargs.append("@get.afni.version")
    cargs.append(version)
    ret = GetAfniVersionOutputs(
        root=execution.output_file("."),
        src_dir=execution.output_file(f"AFNI_{version}/AFNI/src", optional=True),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "GetAfniVersionOutputs",
    "_GET_AFNI_VERSION_METADATA",
    "_get_afni_version",
]
