# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

V_3D_MEDIAN_FILTER_METADATA = Metadata(
    id="330f8a3019a8ce987c14d95e30b507b3ee9df21c",
    name="3dMedianFilter",
    container_image_type="docker",
    container_image_tag="fcpindi/c-pac:latest",
)


class V3dMedianFilterOutputs(typing.NamedTuple):
    """
    Output object returned when calling `v_3d_median_filter(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    output_brik: OutputPathType | None
    """Output dataset is stored in float format."""
    output_head: OutputPathType | None
    """Output dataset header file."""


def v_3d_median_filter(
    dataset: InputPathType,
    irad: float | int | None = None,
    iter_: float | int | None = None,
    verbose: bool = False,
    prefix: str | None = None,
    automask: bool = False,
    runner: Runner | None = None,
) -> V3dMedianFilterOutputs:
    """
    3dMedianFilter by AFNI Team.
    
    Computes the median in a spherical neighborhood around each point in the
    input to produce the output.
    
    More information:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/3dMedianFilter.html
    
    Args:
        dataset: Input dataset.
        irad: Radius in voxels of spherical regions.
        iter_: Iterate 'n' times [default=1].
        verbose: Be verbose during run.
        prefix: Use 'pp' for prefix of output dataset.
        automask: Create a mask (a la 3dAutomask).
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `V3dMedianFilterOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(V_3D_MEDIAN_FILTER_METADATA)
    cargs = []
    cargs.append("3dMedianFilter")
    if irad is not None:
        cargs.extend(["-irad", str(irad)])
    if iter_ is not None:
        cargs.extend(["-iter", str(iter_)])
    if verbose:
        cargs.append("-verb")
    if prefix is not None:
        cargs.extend(["-prefix", prefix])
    if automask:
        cargs.append("-automask")
    cargs.append(execution.input_file(dataset))
    ret = V3dMedianFilterOutputs(
        root=execution.output_file("."),
        output_brik=execution.output_file(f"{prefix}+tlrc.BRIK", optional=True) if prefix is not None else None,
        output_head=execution.output_file(f"{prefix}+tlrc.HEAD", optional=True) if prefix is not None else None,
    )
    execution.run(cargs)
    return ret


__all__ = [
    "V3dMedianFilterOutputs",
    "V_3D_MEDIAN_FILTER_METADATA",
    "v_3d_median_filter",
]
