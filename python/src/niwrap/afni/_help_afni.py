# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_HELP_AFNI_METADATA = Metadata(
    id="c9c699eef1341ac9b5b3f407359029efe99ff6e2",
    name="@help.AFNI",
)


class HelpAfniOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_help_afni(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def _help_afni(
    match: str | None = None,
    lynx: bool = False,
    vi: bool = False,
    less: bool = False,
    nedit: bool = False,
    noview: bool = False,
    runner: Runner | None = None,
) -> HelpAfniOutputs:
    """
    @help.AFNI by AFNI Development Team.
    
    A script to retrieve and search AFNI's help page for all programs.
    
    Args:
        match: Looks for occurrence of each word in the list in the help file.\
            For a match with multiple words, all the words must be on the same line\
            of text in the help file.
        lynx: Set viewer to lynx.
        vi: Set viewer to vi.
        less: Set viewer to less (default).
        nedit: Set viewer to nedit.
        noview: Set viewer to no view.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `HelpAfniOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_HELP_AFNI_METADATA)
    cargs = []
    cargs.append("@help.AFNI")
    cargs.append("[OPTIONS]")
    ret = HelpAfniOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "HelpAfniOutputs",
    "_HELP_AFNI_METADATA",
    "_help_afni",
]
