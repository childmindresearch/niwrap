# This file was auto generated by Styx.
# Do not edit this file directly.

import typing
import pathlib
from styxdefs import *
import dataclasses

INSTALL_MACAQUE_DEMO_METADATA = Metadata(
    id="750f2fb2be92a0f7dccdad67c5cbb0ff37ec8842.boutiques",
    name="Install_MACAQUE_DEMO",
    package="afni",
    container_image_tag="afni/afni_make_build:AFNI_24.2.06",
)


class InstallMacaqueDemoOutputs(typing.NamedTuple):
    """
    Output object returned when calling `install_macaque_demo(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def install_macaque_demo(
    wget: bool = False,
    curl: bool = False,
    runner: Runner | None = None,
) -> InstallMacaqueDemoOutputs:
    """
    Installs the demo archive for AFNI's macaque-analysis demo.
    
    Author: AFNI Team
    
    URL:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/@Install_MACAQUE_DEMO.html
    
    Args:
        wget: Use wget to download archive. Script chooses by default with\
            preference for curl.
        curl: Use curl to download archive. Script chooses by default with\
            preference for curl.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `InstallMacaqueDemoOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(INSTALL_MACAQUE_DEMO_METADATA)
    cargs = []
    cargs.append("Install_MACAQUE_DEMO")
    if wget:
        cargs.append("-wget")
    if curl:
        cargs.append("-curl")
    ret = InstallMacaqueDemoOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "INSTALL_MACAQUE_DEMO_METADATA",
    "InstallMacaqueDemoOutputs",
    "install_macaque_demo",
]
