# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

TO_RAI_METADATA = Metadata(
    id="29f84848df78362516269d6b386a4ebe7db63fa6",
    name="ToRAI",
)


class ToRaiOutputs(typing.NamedTuple):
    """
    Output object returned when calling `to_rai(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def to_rai(
    coordinates: list[float | int],
    orientation: str,
    runner: Runner | None = None,
) -> ToRaiOutputs:
    """
    ToRAI by Unknown.
    
    Tool to change the ORIENT coordinates to RAI.
    
    Args:
        coordinates: Specify the X, Y, and Z coordinates.
        orientation: Specify the orientation.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `ToRaiOutputs`).
    """
    runner = runner or get_global_runner()
    if (len(coordinates) != 3): 
        raise ValueError(f"Length of 'coordinates' must be 3 but was {len(coordinates)}")
    execution = runner.start_execution(TO_RAI_METADATA)
    cargs = []
    cargs.append("@ToRAI")
    cargs.append("<-xyz")
    cargs.append("X")
    cargs.append("Y")
    cargs.append("Z>")
    cargs.append("<-or")
    cargs.append("ORIENT>")
    ret = ToRaiOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "TO_RAI_METADATA",
    "ToRaiOutputs",
    "to_rai",
]
