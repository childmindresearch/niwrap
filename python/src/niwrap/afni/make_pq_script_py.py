# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

MAKE_PQ_SCRIPT_METADATA = Metadata(
    id="e4426b276f6a8ad1ef7c78bf1084b54c5115f496",
    name="make_pq_script",
    container_image_type="docker",
    container_image_tag="fcpindi/c-pac:latest",
)


class MakePqScriptOutputs(typing.NamedTuple):
    """
    Output object returned when calling `make_pq_script(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    script: OutputPathType
    """Generated output script"""


def make_pq_script(
    dataset: InputPathType,
    brick_index: float | int,
    mask: InputPathType,
    out_script: str,
    runner: Runner | None = None,
) -> MakePqScriptOutputs:
    """
    make_pq_script by AFNI Team.
    
    Creates a script to compute p-value and q-value curves.
    
    More information:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/make_pq_script.py.html
    
    Args:
        dataset: Input dataset (no sub-brick selectors).
        brick_index: Volume sub-brick for specific t-stat.
        mask: Mask volume dataset.
        out_script: Name for output script to write.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `MakePqScriptOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(MAKE_PQ_SCRIPT_METADATA)
    cargs = []
    cargs.append("make_pq_script.py")
    cargs.append(execution.input_file(dataset))
    cargs.append(str(brick_index))
    cargs.append(execution.input_file(mask))
    cargs.append(out_script)
    ret = MakePqScriptOutputs(
        root=execution.output_file("."),
        script=execution.output_file(f"{out_script}"),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "MAKE_PQ_SCRIPT_METADATA",
    "MakePqScriptOutputs",
    "make_pq_script",
]
