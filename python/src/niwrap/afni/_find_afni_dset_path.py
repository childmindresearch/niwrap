# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

FIND_AFNI_DSET_PATH_METADATA = Metadata(
    id="f4e005c8766bdf081a9563abf27c34fc079404d3",
    name="FindAfniDsetPath",
)


class FindAfniDsetPathOutputs(typing.NamedTuple):
    """
    Output object returned when calling `find_afni_dset_path(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def find_afni_dset_path(
    dsetname: str,
    append_file: bool = False,
    full_path: bool = False,
    help_: bool = False,
    runner: Runner | None = None,
) -> FindAfniDsetPathOutputs:
    """
    FindAfniDsetPath by Jason W. Bacon, Medical College of Wisconsin.
    
    Searches various AFNI directories for a specified dataset and returns its
    path.
    
    Args:
        dsetname: Name of the dataset to search for.
        append_file: Show the file appended to (even with atlas name).
        full_path: Print full path instead of '.'.
        help_: Display help message.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `FindAfniDsetPathOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(FIND_AFNI_DSET_PATH_METADATA)
    cargs = []
    cargs.append("@FindAfniDsetPath")
    cargs.append(dsetname)
    if append_file:
        cargs.append("-append_file")
    if full_path:
        cargs.append("-full_path")
    if help_:
        cargs.append("-help")
    ret = FindAfniDsetPathOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "FIND_AFNI_DSET_PATH_METADATA",
    "FindAfniDsetPathOutputs",
    "find_afni_dset_path",
]
