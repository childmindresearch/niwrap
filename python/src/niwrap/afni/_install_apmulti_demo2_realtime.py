# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_INSTALL_APMULTI_DEMO2_REALTIME_METADATA = Metadata(
    id="af53b3a7554ce542002c99643d52de57e8764aa6",
    name="@Install_APMULTI_Demo2_realtime",
)


class InstallApmultiDemo2RealtimeOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_install_apmulti_demo2_realtime(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def _install_apmulti_demo2_realtime(
    wget: bool = False,
    curl: bool = False,
    runner: Runner | None = None,
) -> InstallApmultiDemo2RealtimeOutputs:
    """
    @Install_APMULTI_Demo2_realtime by AFNI Development Team.
    
    Fetches the demo data and scripts corresponding to AFNI's Demo #2 for
    experimenting with AFNI's real-time system.
    
    More information: https://afni.nimh.nih.gov
    
    Args:
        wget: Use wget to download the archive. By default, the script prefers\
            curl.
        curl: Use curl to download the archive. By default, the script prefers\
            curl.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `InstallApmultiDemo2RealtimeOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_INSTALL_APMULTI_DEMO2_REALTIME_METADATA)
    cargs = []
    cargs.append("@Install_APMULTI_Demo2_realtime")
    if curl:
        cargs.append("-curl")
    ret = InstallApmultiDemo2RealtimeOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "InstallApmultiDemo2RealtimeOutputs",
    "_INSTALL_APMULTI_DEMO2_REALTIME_METADATA",
    "_install_apmulti_demo2_realtime",
]
