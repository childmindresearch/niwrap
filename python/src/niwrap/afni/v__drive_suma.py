# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

V__DRIVE_SUMA_METADATA = Metadata(
    id="0edd4f09572a71b96709ab4ac60976e4939b3f9f",
    name="@DriveSuma",
    container_image_type="docker",
    container_image_tag="fcpindi/c-pac:latest",
)


class VDriveSumaOutputs(typing.NamedTuple):
    """
    Output object returned when calling `v__drive_suma(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def v__drive_suma(
    h_web: bool = False,
    hweb: bool = False,
    h_view: bool = False,
    hview: bool = False,
    all_opts: bool = False,
    h_find: str | None = None,
    h_raw: bool = False,
    h_txt: bool = False,
    h_spx: bool = False,
    h_aspx: bool = False,
    runner: Runner | None = None,
) -> VDriveSumaOutputs:
    """
    @DriveSuma by AFNI Team.
    
    A script to demonstrate how to drive suma from the command line.
    
    More information:
    https://afni.nimh.nih.gov/pub/dist/doc/program_help/@DriveSuma.html
    
    Args:
        h_web: Open webpage with help for this program.
        hweb: Same as -h_web.
        h_view: Open -help output in a GUI editor.
        hview: Same as -h_view.
        all_opts: List all of the options for this script.
        h_find: Search for lines containing WORD in -help output. Search is\
            approximate.
        h_raw: Raw help string.
        h_txt: Text formatted help string.
        h_spx: Sphinx formatted help string.
        h_aspx: Sphinx formatted help string with auto tagging of options.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `VDriveSumaOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(V__DRIVE_SUMA_METADATA)
    cargs = []
    cargs.append("@DriveSuma")
    if h_aspx:
        cargs.append("-h_aspx")
    ret = VDriveSumaOutputs(
        root=execution.output_file("."),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "VDriveSumaOutputs",
    "V__DRIVE_SUMA_METADATA",
    "v__drive_suma",
]
