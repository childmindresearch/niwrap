# This file was auto generated by Styx.
# Do not edit this file directly.

from styxdefs import *
import pathlib
import typing

_GET_AFNI_ORIENT_METADATA = Metadata(
    id="794be3db9b9a380180fa8bf19bc6a2ba5602e059",
    name="@GetAfniOrient",
    container_image_type="docker",
    container_image_index="index.docker.io",
    container_image_tag="afni/afni_latest",
)


class GetAfniOrientOutputs(typing.NamedTuple):
    """
    Output object returned when calling `_get_afni_orient(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    output_orient_code: OutputPathType
    """File containing the orientation code"""


def _get_afni_orient(
    infile: InputPathType,
    exploratory: bool = False,
    runner: Runner | None = None,
) -> GetAfniOrientOutputs:
    """
    @GetAfniOrient by Ziad Saad (saadz@mail.nih.gov).
    
    Returns the orient code of AFNI datasets.
    
    More information: https://afni.nimh.nih.gov/
    
    Args:
        infile: Input AFNI dataset (e.g. Hello+orig.HEAD).
        exploratory: Exploratory flag for additional functionalities.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `GetAfniOrientOutputs`).
    """
    runner = runner or get_global_runner()
    execution = runner.start_execution(_GET_AFNI_ORIENT_METADATA)
    cargs = []
    cargs.append("@GetAfniOrient")
    if exploratory:
        cargs.append("-exp")
    cargs.append(execution.input_file(infile))
    ret = GetAfniOrientOutputs(
        root=execution.output_file("."),
        output_orient_code=execution.output_file(f"{pathlib.Path(infile).name}_orient_code.txt", optional=True),
    )
    execution.run(cargs)
    return ret


__all__ = [
    "GetAfniOrientOutputs",
    "_GET_AFNI_ORIENT_METADATA",
    "_get_afni_orient",
]
